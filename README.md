# Oberon Web (Lite MVP)

Phone-first web dashboard (PWA style) with:
- Google OAuth sign-in
- Gmail read-only scan -> Urgent / Fix Required cards
- Google Calendar read-only -> Upcoming events

## Setup
1) Install Node.js 18+
2) npm install
3) Copy .env.example -> .env.local and fill values.
4) npm run dev
